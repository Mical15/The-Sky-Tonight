import React from "react";

// const data = Array.from(props.data);

const Planets = props => {
    return (
        <div>
            {/* <div>
                {Object.keys(props.data[0]).forEach(key => 
                    <p>{key}</p>)}
            </div> */}
{/* 
            <h1>{console.log(props.data)}</h1> */}
            {/* {props.data.map((data) => {
                <p>
                    {data.name} + {" /n"} */}
                    {/* props.data.constellation, response.data.data[i].constellation,
                    props.data.ra, response.data.data[i].rightAscension.hours + 'h ' + response.data.data[i].rightAscension.minutes + 'm ' + response.data.data[i].rightAscension.seconds + 's',
                    props.data.dec, response.data.data[i].degrees + '\u00B0 ' + response.data.data[i].arcminutes + `' ` + response.data.data[i].arcseconds + `" ` */}
                {/* </p>
            })} */}

        </div>
    )
}

export default Planets;